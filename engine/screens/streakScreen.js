// Pantalla: streakScreen — vista detallada de la racha. Se llena más adelante.
export default {
  render(container) {
    container.innerHTML = `<div class="tarjeta"><h1 class="display">streakScreen</h1></div>`;
  }
};
